<% content_for :title, "New entry | Admin" %>
<% content_for :body_class, "admin-page" %>

<main class="shell shell--admin">
  <%= render "admin/shared/header" %>

  <%= render "admin/shared/heading", title: "New entry", action: nil %>

  <%= render "form", entry: @entry %>
</main>
